{
  "name": "is-leap-year",
  "version": "0.1.0",
  "description": "Check if a date is in a leap year",
  "author": "Andrew Gerard <andrew@kinetifex.com> (http://www.kinetifex.com)",
  "license": "MIT",
  "scripts": {
    "eslint": "godaddy-js-style-eslint --es5 *.js",
    "jscs": "godaddy-js-style-jscs --es5 *.js",
    "pretest": "npm run eslint && npm run jscs",
    "build": "npm run prepublish && npm run pretest",
    "test": "mocha"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:kinetifex/is-leap-year.git"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "godaddy-style": "^3.1.6",
    "mocha": "^3.2.0"
  }
}
